<odoo>
  <data>
    <!--Fuchilfte für Verkaufsaufträge-->
    <record id="view_sale_order_inherit_search" model="ir.ui.view">
      <field name="name">sale.order.search.expand.filter</field>
      <field name="model">sale.order</field>
      <field name="inherit_id" ref="sale.sale_order_view_search_inherit_sale"/>
      <field name="arch" type="xml">
        <xpath expr="//search" position="inside">
          <!-- This field will show up on the default filters
               &lt; is the XML encoding for < and &gt; is the XML encoding for > -->
          <filter string="Total &lt; 1000" name="total_under_1000" domain="[('amount_total', '&lt;', 1000)]"/>
          <filter string="Total &gt;= 1000" name="total_above_1000" domain="[('amount_total', '&gt;=', 1000)]"/>
        </xpath>
      </field>
    </record>
<!--#####################################################################################-->
    <!--Fuchilfte für Verkaufsaufträge-->
     <record id="view_purchase_order_inherit_search" model="ir.ui.view">
      <field name="name">purchase.order.search.expand.filter</field>
      <field name="model">purchase.order</field>
      <field name="inherit_id" ref="purchase.view_purchase_order_filter"/>
      <field name="arch" type="xml">
        <xpath expr="//search" position="inside">
          <!-- This field will show up on the default filters
               &lt; is the XML encoding for < and &gt; is the XML encoding for > -->
        <!--<filter string="Verspäteddt" name="toolddate" domain="[('is_shipped','=',False),('x_ABzum','!=',False),('x_ABzum','<=', datetime.today())]"/>-->
           <filter string="Verspätet" name="toolate" domain="[('x_ABzum','&lt;', datetime.datetime.today()),('is_geliefert','=',False)]"/>
            <filter string="Geliefert" name="toolate" domain="[('is_geliefert','=',True)]"/>
        </xpath>
      </field>
    </record>


      <!--Inherit the sale order form view-->
      <record id="view_purchase_tree_custom" model="ir.ui.view">
        <field name="name">purchase.order.custom.form.inherited</field>
     <field name="model">purchase.order</field>
     <field name="inherit_id" ref="purchase.purchase_order_tree"/>
     <field name="arch" type="xml">
          <xpath expr="//tree" position="attributes">
              <attribute name="colors"> red: not is_geliefert and x_ABzum and x_ABzum &lt; current_date </attribute>
              <attribute name="colors"> red: not x_ABzum and date_planned &lt; current_date </attribute>

          </xpath>
     </field>
</record>



  </data>
</odoo>

<!--colors=„red: not is_shipped and x_ABzum and x_ABzum &lt; current_date or not is_shipped and not x_ABzum and date_planned &lt; current_date “-->